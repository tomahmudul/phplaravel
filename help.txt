This two line of code will be run only once
git config --global user.name " "
git config --global user.email " "

Create the Project and well, test it.
run git init
run git add
run git commit

Open GitHub and create a new repo with the master branch.
run git branch -M main locally
run git remote add origin remote_repo_url
run git push -u origin main